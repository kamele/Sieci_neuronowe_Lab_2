from Warstwa import *

if __name__ == '__main__':
    # dane_treningowe = Warstwa.wczytajDane("train-images.idx3-ubyte")
    # klasy_treningowe = Warstwa.wczytajKlasy("train-labels.idx1-ubyte")
    # #
    # # warstwa_wejściowa= Warstwa(2, Warstwa.sigmoidalna)
    # # warstwa_wejściowa.wylicz(dane_treningowe,klasy_treningowe)
    # liczby_neuronow = [2,3,10]
    # funkcje_aktywacji = [tanh, ReLu, softmax]
    # MLP(3, dane_treningowe, liczby_neuronow, funkcje_aktywacji)
    Warstwa.test()

